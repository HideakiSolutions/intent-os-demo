{
  "_intent_os": {
    "intent_id": "5ff27691-7d67-47be-a70e-bed40784bab7",
    "version": 1,
    "_do_not_edit": "Generated by intent-os harness. Edits override on next bundle pull."
  },
  "mcpServers": {
    "intent-os-context": {
      "command": "node",
      "args": ["${INTENT_OS_REPO}/apps/mcp-intent-os-context/dist/server.js"],
      "env": {
        "HARNESS_ADDR": "${HARNESS_ADDR}",
        "POLICY_ADDR": "${POLICY_ADDR}",
        "CONTEXT_ADDR": "${CONTEXT_ADDR}",
        "AUDIT_ADDR": "${AUDIT_ADDR}",
        "INTENT_OS_AGENT_TOKEN": "${INTENT_OS_AGENT_TOKEN}",
        "INTENT_OS_AGENT_SECRET": "${INTENT_OS_AGENT_SECRET}"
      }
    }
  }
}
